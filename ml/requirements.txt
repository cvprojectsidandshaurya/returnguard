# Python 3.12. PyTorch has no wheels for 3.13 or 3.14, so a venv built on a
# newer interpreter fails at `pip install torch`.
#
# On Apple Silicon, build the venv with the NATIVE arm64 Homebrew python.
# A Rosetta Homebrew under /usr/local caps torch at 2.2.2 and loses MPS, and
# `which brew` may well point at it. Check with:
#
#   file "$(brew --prefix python@3.12)/bin/python3.12"   # must say arm64
#
#   /opt/homebrew/bin/brew install python@3.12
#   /opt/homebrew/opt/python@3.12/bin/python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r ml/requirements.txt

# Core
torch>=2.4
torchvision>=0.19
numpy>=1.26,<3
pandas>=2.2,<3
pillow>=10.3
scikit-learn>=1.5

# Models
transformers>=4.44
open_clip_torch>=2.24

# Notebooks and plots
jupyterlab>=4.2
matplotlib>=3.9

# Tooling
tqdm>=4.66
pytest>=8.2
