# Python 3.12 is required. PyTorch has no wheels for 3.13 or 3.14 yet, so a
# venv built on a newer interpreter will fail at `pip install torch`.
#
#   brew install python@3.12
#   "$(brew --prefix python@3.12)/bin/python3.12" -m venv .venv
#   source .venv/bin/activate
#   pip install -r ml/requirements.txt

# Core
torch>=2.4
torchvision>=0.19
numpy>=1.26,<3
pandas>=2.2,<3
pillow>=10.3
scikit-learn>=1.5
opencv-python>=4.10

# Models
transformers>=4.44
open_clip_torch>=2.24

# Notebooks and plots
jupyterlab>=4.2
matplotlib>=3.9

# Tooling
tqdm>=4.66
pytest>=8.2
